# Claude Code local settings (machine-specific)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Build / deps
node_modules/
vendor/
*.log

# WordPress local artifacts
wp-cli.local.yml

# Build artifacts (zips) — never track release packages in the repo
Plugin Zip files/
*.zip
